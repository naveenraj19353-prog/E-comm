# Include inside the api.retailcosmos.com server { } block, then reload nginx.
# Default nginx client_max_body_size is 1m and returns 413 for banner videos.

client_max_body_size 64m;
client_body_timeout 120s;
proxy_read_timeout 120s;
proxy_send_timeout 120s;
