import { buildDefaultFooterContent } from "../../theme/footerDefaults";
import type { FooterProps } from "./types";

export const footerData: FooterProps = buildDefaultFooterContent("Retail Cosmos");
